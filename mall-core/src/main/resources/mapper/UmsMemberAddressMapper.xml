<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wxq.mall.mapper.UmsMemberAddressMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="baseMap" type="com.wxq.mall.model.UmsMemberAddress">
        <id column="id" property="id" />
        <result column="member_id" property="memberId" />
        <result column="receive_name" property="receiveName" />
        <result column="receive_phone" property="receivePhone" />
        <result column="province_id" property="provinceId" />
        <result column="city_id" property="cityId" />
        <result column="region_id" property="regionId" />
        <result column="detail_address" property="detailAddress" />
    </resultMap>


    <select id="findByPage" resultMap="baseMap">
        select id,member_id,receive_name,receive_phone,province_id,city_id,region_id,detail_address
        from ums_member_address
    </select>

    <select id="findAll" resultMap="baseMap">
        select id,member_id,receive_name,receive_phone,province_id,city_id,region_id,detail_address
        from ums_member_address
    </select>
</mapper>
